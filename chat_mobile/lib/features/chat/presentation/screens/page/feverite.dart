
import 'package:flutter/material.dart';

class FeveritePage extends StatelessWidget {
  const FeveritePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Feverite Page'),
      ),
    );
  }
}